server:
  servlet:
    session:
      cookie:
        name: JESSIONID
spring:
  jwt:
    secret: alksdjklasdjlkasdjlaskjdklasjdlkasjdlkasjdlksajdlkasjdlksajldks
  data:
    h2:
      console:
        enabled: true
        path: /h2-console
    datasource:
      driver-class-name: org.h2.Driver
      url: jdbc:h2:survey:db;
      username: survey
      password: password
    jpa:
      hibernate:
        ddl-auto: create
      properties:
        hibernate:
          format_sql: true
    redis:
      host: localhost
      port: 6379
  session:
    store-type: redis
    redis:
      flush-mode: on_save # Redis에 기록
      namespace: survey:session